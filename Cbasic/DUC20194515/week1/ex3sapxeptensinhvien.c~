#include<stdio.h>
#include<string.h>
#define MAX 100
char ten(char str[MAX]){
  char name[MAX];
  int i;
  for(i=strlen(str);str[i] != ' ';--i){
    strcpy(name,str[i-1]);
  }
  return name;
}
int main(){
  
}
