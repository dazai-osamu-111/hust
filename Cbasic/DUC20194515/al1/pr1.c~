
#include<stdio.h>
int main(){
  int a[100];
  int i,j,k,maxSum = 0;
    for(i=0;i<n;i++){
      for(j=0;j<n;j++){
	int sum=0;
	for(k=i;k<=j;k++)
	  sum=sum+a[k]'
	    if(sum>maxsum) maxsum=sum;
      }	     
		     
    }
    return 0;
}
