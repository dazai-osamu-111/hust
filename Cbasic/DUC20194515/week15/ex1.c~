#include<stdio.h>
void insertsionsort(int a[], int n){
  int i,j;
  int next;
  for(i=1;i<n;i++){
    next=a[i];
    for(j=i-1;j>=0 && next<a[i];j--)
      a[j+1]=a[j];
    a[j+1]=next;
  } 
}
int main(){
  
}
