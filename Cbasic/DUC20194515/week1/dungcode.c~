#include<stdio.h>
#include<string.h>

int main() {
  char  hoten[100];
  int i;
  char s[100];
  printf("nhap ho va ten");
  fgets(hoten,sizeof(hoten),stdin);
  for(i=strlen(hoten)-1;hoten[i]!=' ';i--){
    strcpy(s,&hoten[i]);
    }
  puts(s);
  return 0;
}
