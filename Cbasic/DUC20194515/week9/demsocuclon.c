#include<stdio.h>
#include "stackrec.h"
int main(){
  char S1[100], S2[100];
  int i;
  printf("Enter first number: ");
  scanf("%s", s1);
  printf("Enter second number: ");
  scanf("%s", s2);
  stacktype s1, s2, s3;
  initialize(s1);
  initialize(s2);
  initialize(s3);
  for(i=0;str
}
