#include<stdio.h>
#define MAX 100
int main(){
    int a[MAX],n,i,j,k;
    int maxsum = 0;
    printf("Nhap n: ");
    scanf("%d", &n);
    for(i=0;i<n;++){
	printf("Nhap a[%d] ",i+1);
	scanf("%d", &a[i]);
    }
    for(i=0;i<n;i++){

    }
}
